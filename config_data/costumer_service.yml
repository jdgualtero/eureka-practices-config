#Application Configuration
server:
  port: 8092

spring:
    datasource:
        password: augeAdmin123
        url: jdbc:postgresql://ip-172-31-6-64.us-east-2.compute.internal/postgres
        username: postgres
    jpa:
        generate-ddl: true
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
                jdbc:
                    lob:
                        non_contextual_creation: true
    
    
eureka:
    client:
        serviceUrl:
            defaultZone: http://ec2-13-59-81-251.us-east-2.compute.amazonaws.com:8099/eureka/
logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} ${LOG_LEVEL_PATTERN:-%5p} %m%n"
